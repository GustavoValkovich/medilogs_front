/**
 * Polyfills required by Angular. This file is loaded before the app and
 * should include any global polyfills your app needs.
 */

// Zone JS is required by default for Angular's change detection.
import 'zone.js';

// Optional: custom elements polyfill for supporting Web Components in older browsers
// (project already depends on @webcomponents/custom-elements in package.json)
import '@webcomponents/custom-elements';
